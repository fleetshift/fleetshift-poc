This is code that is intended to run in a separate verifier process, i.e. delivery agents responsible for receiving attested manifests and placement, validating, and applying, if valid.

It is hosted here inside the server for demonstration / prototype purposes only.