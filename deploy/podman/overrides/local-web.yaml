# Bind host web/ over the AIO baked UI. Layered by LOCAL_WEB=true.
# !override replaces the whole volumes list, so every base mount is restated.

services:

  fleetshift-server:
    volumes: !override
      - ${PODMAN_SOCKET}:/var/run/docker.sock
      - /tmp:/tmp
      - fleetshift-data:/data
      - ../../web:/srv/web:ro,z
